/*
automatic program to find the amount to be given based on age and service period 
using functions and initiaalization

*/
function abc(age,amount)
{
    maxage=100;
    console.log((maxage*360)*(maxage-age))
}
var abc=new abc(10,20)